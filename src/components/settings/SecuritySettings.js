import React from "react";

import classes from "./SecuritySettings.module.scss";

const SecuritySettings = (props) => {
  return <div className={`${classes.container}`}>SecuritySettings</div>;
};

export default SecuritySettings;
