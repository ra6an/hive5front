import React from "react";

import classes from "./AlertSettings.module.scss";

const AlertSettings = (props) => {
  return <div className={`${classes.container}`}>AlertSettings</div>;
};

export default AlertSettings;
